//const - si la variable nunca va a cambiar

const name: string = 'Strider';
let levels: number | string = 52; 
const isStar: boolean = true;

levels = 'FULL';

console.log({
    name, levels, isStar
});

export{};